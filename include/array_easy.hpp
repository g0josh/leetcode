#pragma once
#include <vector>

namespace array_easy{

//Remove duplicates in a sorted array
int removeSortedDuplicates(std::vector<int>&);
//Faster way to remove duplicates in a sorted array
int removeSortedDuplicatesF(std::vector<int>&);

}